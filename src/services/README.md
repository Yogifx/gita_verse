# Services

API clients, data fetching functions, and external service integrations.

- Organize by domain: `content.service.ts`, `auth.service.ts`, etc.
- Keep services framework-agnostic (no React imports).
- Services return typed data; components consume via hooks or server components.
