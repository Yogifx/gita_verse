# Utility Functions

Pure helper functions with no side effects.

- `formatDate.ts`, `slugify.ts`, `validators.ts`, etc.
- Must be framework-agnostic and individually testable.
- For Tailwind/shadcn utilities, see `lib/utils.ts`.
