# Custom Hooks

Reusable React hooks shared across the application.

- `useMediaQuery` — responsive breakpoint detection
- `useDebounce` — debounced value hook
- Place feature-specific hooks inside `features/<name>/hooks/` instead.
